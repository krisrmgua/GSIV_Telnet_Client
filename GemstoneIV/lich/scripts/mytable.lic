if Room.current.id.to_s == "228" or Room.current.id.to_s == "288"
  Script.run('go2', '3743')
  if script.vars[1]
    who = script.vars[1]
    fput "go #{who} table"
    sleep 1
    fput "go #{who} table"
  else
      fput "go table"
  end
end
